// Root layout — minimal shell
// The [locale]/layout.tsx owns fonts, ThemeProvider, and all visual structure.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
