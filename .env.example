# Client-side (NEXT_PUBLIC_* are exposed to the browser)
NEXT_PUBLIC_URL=
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=

# Server-side only (never expose to client)
SANITY_API_TOKEN=
RESEND_API_KEY=
PLAUSIBLE_DOMAIN=
